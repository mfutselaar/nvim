{
  "forin": {
    "body": [
      "for _, $1 := range $2 {",
      "}"
    ],
    "prefix": "forin"
  },
  "iferr": {
    "body": [
      "if err != nil {",
      "    log.Fatal(\"$1\", err)",
      "}"
    ],
    "prefix": "iferr"
  },
  "main": {
    "body": [
      "package main",
      "",
      "func main() {",
      "\tprintln(\"Hello World!\")",
      "}"
    ],
    "prefix": "main"
  },
  "unmarshaljson": {
    "body": [
      "func (target *$1) UnmarshalJSON(data []byte) error {",
      "\ttype Alias $1",
      "\taux := &struct {",
      "\t\t// Check for optional field: Property *type `json:\"property\"`",
      "\t\t*Alias",
      "\t}{",
      "\t\tAlias: (*Alias)(p),",
      "\t}",
      "",
      "\tif err := json.Unmarshal(data, &aux); err != nil {",
      "\t\treturn err",
      "\t}",
      "",
      "    // Optional example:",
      "",
      "\t// if aux.Property == nil {",
      "\t//  \ttarget.Property = true",
      "\t// } else {",
      "\t//  \ttarget.Property = *aux.Property",
      "\t// }",
      "",
      "\treturn nil",
      "}"
    ],
    "prefix": "unmarshaljson"
  }
}
